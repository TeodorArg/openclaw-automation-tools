{
	"id": "openclaw-session-bloat-warning",
	"name": "Session Health Warning: Stay Sharp in Long AI Work",
	"description": "Early session-health warnings for OpenClaw. Catch compaction pressure, timeout risk, lane pressure, and no-reply streaks before long AI work slows down.",
	"version": "0.1.3",
	"skills": ["./skills"],
	"configSchema": {
		"type": "object",
		"additionalProperties": false,
		"properties": {
			"stateFilePath": {
				"type": "string",
				"description": "Optional path to the plugin-owned warning state JSON file. Defaults to ./.openclaw-session-bloat-warning-state.json in the current working directory."
			},
			"defaultLanguage": {
				"type": "string",
				"enum": ["en", "ru"],
				"description": "Language for user-facing warning copy. Defaults to en."
			},
			"enablePreCompactionWarning": {
				"type": "boolean",
				"description": "Whether to emit a short warning on before_compaction. Defaults to true."
			},
			"enablePostCompactionNote": {
				"type": "boolean",
				"description": "Whether to emit a short continuation note on after_compaction when the event carries a writable messages array. Defaults to true."
			},
			"maxWarningsPerSession": {
				"type": "integer",
				"minimum": 1,
				"description": "Maximum number of early warnings, pre-compaction warnings, and post-compaction notes per session key. Defaults to 2."
			},
			"enableEarlyWarning": {
				"type": "boolean",
				"description": "Whether to track early session-heaviness, timeout-risk, and lane-pressure signals from llm_input and llm_output and deliver visible warnings on before_agent_reply. Defaults to true."
			},
			"cooldownTurns": {
				"type": "integer",
				"minimum": 1,
				"description": "Minimum turn gap before another early warning can be counted for the same session. Defaults to 3."
			},
			"warningCharThreshold": {
				"type": "integer",
				"minimum": 1,
				"description": "Approximate combined prompt-plus-history character threshold for elevated heaviness classification. Defaults to 120000."
			},
			"warningMessageCountThreshold": {
				"type": "integer",
				"minimum": 1,
				"description": "Approximate history message-count threshold for elevated heaviness classification. Defaults to 80."
			},
			"earlyWarningCharThreshold": {
				"type": "integer",
				"minimum": 1,
				"description": "Approximate combined prompt-plus-history character threshold for earlier visible warning before near-compaction pressure. Defaults to 90000."
			},
			"earlyWarningMessageCountThreshold": {
				"type": "integer",
				"minimum": 1,
				"description": "Approximate history message-count threshold for earlier visible warning before near-compaction pressure. Defaults to 60."
			},
			"warningInputTokensThreshold": {
				"type": "integer",
				"minimum": 1,
				"description": "Observed llm_output usage.input threshold that marks a warning-level heavy turn. Defaults to 120000."
			},
			"elevatedInputTokensThreshold": {
				"type": "integer",
				"minimum": 1,
				"description": "Observed llm_output usage.input threshold that marks an elevated heavy turn. Defaults to 145000."
			},
			"criticalInputTokensThreshold": {
				"type": "integer",
				"minimum": 1,
				"description": "Observed llm_output usage.input threshold that marks a critical heavy turn. Defaults to 170000."
			},
			"timeoutRiskStreakThreshold": {
				"type": "integer",
				"minimum": 1,
				"description": "Number of repeated `timeout_risk` observations required before classifying a `timeout_risk` warning. Defaults to 2."
			},
			"lanePressureStreakThreshold": {
				"type": "integer",
				"minimum": 1,
				"description": "Number of repeated `lane_pressure` observations required before classifying a `lane_pressure` warning. Defaults to 1."
			},
			"noReplyStreakThreshold": {
				"type": "integer",
				"minimum": 1,
				"description": "Number of repeated `no_reply_streak` observations required before classifying a `no_reply_streak` warning. Defaults to 2."
			},
			"timeoutRiskMsThreshold": {
				"type": "integer",
				"minimum": 1,
				"description": "Minimum observed timeout duration used for timeout-risk classification. Defaults to 45000."
			},
			"lanePressureMsThreshold": {
				"type": "integer",
				"minimum": 1,
				"description": "Minimum observed lane wait duration used for lane-pressure classification. Defaults to 10000."
			},
			"contextWindowTokens": {
				"type": "integer",
				"minimum": 1,
				"description": "Approximate model context window used to derive ratio-based token warning thresholds. Defaults to 200000."
			},
			"warningInputTokensRatio": {
				"type": "number",
				"exclusiveMinimum": 0,
				"maximum": 1,
				"description": "Ratio of contextWindowTokens that marks warning-level token pressure. Defaults to 0.6."
			},
			"elevatedInputTokensRatio": {
				"type": "number",
				"exclusiveMinimum": 0,
				"maximum": 1,
				"description": "Ratio of contextWindowTokens that marks elevated token pressure. Defaults to 0.725."
			},
			"criticalInputTokensRatio": {
				"type": "number",
				"exclusiveMinimum": 0,
				"maximum": 1,
				"description": "Ratio of contextWindowTokens that marks critical token pressure. Defaults to 0.85."
			}
		}
	}
}
